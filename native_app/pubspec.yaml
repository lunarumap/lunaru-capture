name: lunaru_capture
publish_to: 'none'
description: LUNARU Capture — guided native 360 capture for photogrammetry and virtual tours.
version: 0.3.0+4

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.12.1
  motion_core: ^0.2.1
  path_provider: ^2.1.6
  path: ^1.9.1
  vector_math: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
